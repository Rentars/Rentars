{
  "name": "rentars",
  "version": "0.1.0",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*"
    ],
    "nohoist": []
  },
  "engines": {
    "node": ">=20.0.0",
    "yarn": ">=1.22.0"
  },
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"blue,green\" \"yarn workspace rentars-backend dev\" \"yarn workspace web dev\"",
    "build": "yarn workspace rentars-backend build && yarn workspace web build",
    "test": "yarn workspace rentars-backend test && yarn workspace web test",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "format": "biome format --write .",
    "clean": "find . -name 'node_modules' -type d -prune -exec rm -rf '{}' + && find . -name 'dist' -type d -prune -exec rm -rf '{}' + && find . -name '.next' -type d -prune -exec rm -rf '{}' +",
    "engines:check": "bash scripts/check-engines.sh",
    "versions": "bash scripts/check-engines.sh",
    "release:validate": "bun run scripts/validate-changelog.ts",
    "release:manifest": "bun run scripts/generate-release-manifest.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@testing-library/dom": "^10.4.1",
    "@types/bun": "latest",
    "@types/express": "^4.17.23",
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^13.3.0"
  },
  "packageManager": "yarn@1.22.22"
}
